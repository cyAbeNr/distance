import math
points = [(xE1, yE1), (xE2, yE2), (xF1, yF1), (xF2, yF2)]  
    q = F[0] - E[0] 
    r = F[1] - E[1]  
    p = math.sqrt(q*2 + r*2)
      distances = []  

return p
or i in range(len(points)):
    for j in range(i + 1, len(points)):  
        E = points[i]
        F = points[j]
        distance = euclideanDistance(E, F)
        distances.append(distance)
min_distance = min(distances)
print("En küçük mesafe:", min_distance)
